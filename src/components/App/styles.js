import styled from 'styled-components';

export const Container = styled.div` //Container que irá englobar e sempre centralizar todas as telas da aplicação
    width: 100%;
    max-width: 500px; //se a tela tiver mais de 500px, ocupa apenas 500px, se n fica no 100% do dispositivo
    margin: 0 auto; //centraliza o container
    padding: 0 16px; //não ficar grudado
`
